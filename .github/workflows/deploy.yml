name: 🚀 Auto Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      - name: 🚀 Deploy via Webhook
        run: |
          curl -sS "https://ebili.online/public/deploy.php?key=Coders123"
